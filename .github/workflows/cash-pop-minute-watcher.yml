name: Michigan Cash Pop persistent watchers

on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/cash-pop-minute-watcher.yml"
      - "v0/minute_watcher.py"
  schedule:
    - cron: "0,30 * * * *"

permissions:
  contents: write

jobs:
  watch:
    runs-on: ubuntu-latest
    timeout-minutes: 58
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install watcher browser
        run: |
          pip install playwright
          playwright install chromium

      - name: Poll continuously every 30 seconds
        run: |
          git config user.name "cash-pop-minute-bot"
          git config user.email "cash-pop-minute-bot@users.noreply.github.com"
          python v0/minute_watcher.py
