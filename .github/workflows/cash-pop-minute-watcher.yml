name: Michigan Cash Pop near-live watcher

on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/cash-pop-minute-watcher.yml"
      - "v0/minute_watcher.py"
  schedule:
    - cron: "*/5 * * * *"

permissions:
  contents: write

concurrency:
  group: cash-pop-near-live-watcher
  cancel-in-progress: true

jobs:
  watch:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install watcher browser
        run: |
          pip install playwright
          playwright install chromium

      - name: Poll every 30 seconds
        run: |
          git config user.name "cash-pop-minute-bot"
          git config user.email "cash-pop-minute-bot@users.noreply.github.com"
          python v0/minute_watcher.py
